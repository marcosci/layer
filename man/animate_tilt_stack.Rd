% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilt_animate.R
\name{animate_tilt_stack}
\alias{animate_tilt_stack}
\title{Animate a tilt stack}
\usage{
animate_tilt_stack(
  stack,
  type = c("unfold", "reveal"),
  direction = c("up", "down"),
  n_frames = 50,
  ...
)
}
\arguments{
\item{stack}{A \code{tilt_stack} object.}

\item{type}{The type of animation: \code{"unfold"} (all layers expand from flat to 3D simultaneously)
or \code{"reveal"} (layers appear one by one).}

\item{direction}{The direction of the animation: \code{"up"} (bottom layer is fixed) or \code{"down"} (top layer is fixed).
Currently only affects \code{"unfold"}.}

\item{n_frames}{Number of frames for the animation.}

\item{...}{Additional arguments passed to \code{gganimate} functions.}
}
\value{
A \code{gganim} object.
}
\description{
Creates a \code{gganimate} object that visualizes the "infolding" or "revealing" of a map stack.
}
\examples{
\donttest{
# CRAN-compliant parallel example
if (requireNamespace("mirai", quietly = TRUE)) {
  # Try to set up a daemon, but handle cases where it might be blocked
  # (e.g., by some system security profiles)
  daemons_started <- tryCatch({
    mirai::daemons(1, dispatcher = FALSE)
    TRUE
  }, error = function(e) FALSE)
  
  if (daemons_started) {
    stack <- tilt_stack() |>
      tilt_layer(landscape_1) |>
      tilt_layer(landscape_2)
      
    anim <- animate_tilt_stack(stack, n_frames = 5)
    
    mirai::daemons(0)
    Sys.sleep(1)
  }
}
}
}

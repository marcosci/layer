% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilt_lines.R
\name{tilt_lines}
\alias{tilt_lines}
\title{Create lines connecting corresponding features between two tilted maps}
\usage{
tilt_lines(data1, data2)
}
\arguments{
\item{data1}{An \code{sf} object (e.g., points or polygons).}

\item{data2}{An \code{sf} object with the same number of rows as \code{data1}.}
}
\value{
An \code{sf} object containing \code{LINESTRING} geometries connecting the features
of \code{data1} and \code{data2}. Non-spatial attributes from \code{data1} are preserved.
}
\description{
This function creates \code{LINESTRING} geometries connecting the features of two
identically-structured \code{sf} objects. It is designed to connect points or polygon
centroids between layers that have been shifted differently using \code{tilt_map()}.
}
\examples{
\donttest{
# create points
pts <- data.frame(x = c(1, 2), y = c(1, 2), id = c(1, 2))
pts <- sf::st_as_sf(pts, coords = c("x", "y"))

# tilt and shift
pts_tilt1 <- tilt_map(pts)
pts_tilt2 <- tilt_map(pts, x_shift = 10, y_shift = 10)

# connect
lines <- tilt_lines(pts_tilt1, pts_tilt2)

# plot
plot_tiltedmaps(list(pts_tilt1, lines, pts_tilt2))
}
}
